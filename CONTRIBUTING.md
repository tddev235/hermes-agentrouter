# Contributing

1. Never commit API keys or local Hermes state.
2. Keep `glm-5.2` as the tested default unless a change is discussed first.
3. Run `python tests/test_structure.py` and the plugin validator before opening a pull request.
4. Test installer changes in an isolated user profile or disposable environment.

